1.The code is in the "resource code" file folder. In the folder, the "app" file folder is filled 
with front-end web code, it should be imported into the Visual Studio Code to start. 

And the "server" file folder is filled with the Java back-end code, it should be imported
into the IntelliJ IDEA to start.

The "sample.sql" should be imported into the Navicat 15 for MySQL first to create the 
database and tables in it.